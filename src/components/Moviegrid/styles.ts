import styled from "styled-components";

export const Container = styled.div`
  height: 22rem;

  div > h1 { 
    color: var(--font-color);
    font-weight: bold;
    font-size: 2.5rem;
    margin-bottom: .5rem;
    padding-left: 2.2rem;
  }
`